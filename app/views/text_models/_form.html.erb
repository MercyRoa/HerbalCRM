<%= form_for(@text_model) do |f| %>
  <% if @text_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_model.errors.count, "error") %> prohibited this text_model from being saved:</h2>

      <ul>
      <% @text_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
